# Copy to .env.local and paste your ArcGIS API key (see README → Deployment).
# The app runs without it, but ArcGIS scene demos then show a friendly
# "API key required" notice instead of premium basemaps/layers.
VITE_ARCGIS_API_KEY=
